version: '3.8'

services:
  payments-api:
    container_name: trello-clone-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules  # previene conflictos con los node_modules locales

    command: npm run dev
    develop:
      watch:
        - action: rebuild 
          path: ./src
          target: /usr/src/app/src
    